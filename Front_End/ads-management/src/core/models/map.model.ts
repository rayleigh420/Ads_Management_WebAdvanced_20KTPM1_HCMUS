export interface Coordinates {
    longitude: number,
    latitude: number
}

export interface CoordinatesPopup {
    title: string,
    description: string,
    location: string,
    status: string
}
