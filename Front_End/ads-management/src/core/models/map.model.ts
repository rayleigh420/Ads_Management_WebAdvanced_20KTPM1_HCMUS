export interface LocationInfo {
    longitude: number,
    latitude: number
}

export interface LocationInfoPopup {
    title: string,
    description: string,
    location: string,
    status: string
}